excluded:
  - Pods
disabled_rules:
  - trailing_whitespace
  - line_length
  - vertical_whitespace
  - unused_optional_binding
identifier_name:
  excluded: # excluded via string array
    - id
    - URL
    - url
    - app
file_length:
  warning: 500
  error: 800
type_body_length:
  - 500 # warning
  - 700 # error
